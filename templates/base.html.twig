<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}A&K Pizza's{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #1a1a1a; /* Donkere achtergrond */
            color: white;
        }
        .navbar {
            background-color: #c0392b; /* Rood */
        }
        .navbar-brand img {
            width: 40px;
            margin-right: 10px;
        }
        .nav-link {
            color: white !important;
        }
        .nav-link:hover {
            color: #f39c12 !important; /* Geel effect */
        }
        .btn-auth {
            color: white !important;
            border: 1px solid white;
        }
        .btn-auth:hover {
            background-color: #f39c12 !important;
            border-color: #f39c12 !important;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-danger">
    <div class="container">
        <a class="navbar-brand" href="{{ path('pizza_index') }}">
            <img src="{{ asset('img/logo.png') }}" alt="Logo" height="40"> A&K Pizza's
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('pizza_index') }}">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('pizza_categories') }}">Categorieën</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('order_cart') }}">Winkelwagen</a></li>

                {% if app.user %}
                    <li class="nav-item"><a class="btn btn-outline-light" href="{{ path('app_logout') }}">Uitloggen</a></li>
                {% else %}
                    <li class="nav-item"><a class="btn btn-light me-2" href="{{ path('app_register') }}">Registreren</a></li>
                    <li class="nav-item"><a class="btn btn-outline-light" href="{{ path('app_login') }}">Inloggen</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
